app:
  use_single_mode: true

models:
  default:
    provider: ollama
    model_name: qwen2.5:0.5b
    temperature: 0.0
    base_url: http://localhost:11434

  default_vl:
    provider: ollama
    model_name: qwen2.5:0.5b # Fallback to text-only model due to low memory
    temperature: 0.0
    base_url: http://localhost:11434
    num_ctx: 1024 # Increased slightly as model is small

database:
  host: localhost
  port: 27017
  name: hanachan_db

tools:
#   mcp_servers:
#     qdrant:
#       command: uvx
#       args: 
#         - mcp-server-qdrant
#       env:
#         QDRANT_LOCAL_PATH: ~/.hanachan/qdrant_db
#         COLLECTION_NAME: hanachan_grammar
#         EMBEDDING_MODEL: sentence-transformers/all-MiniLM-L6-v2
#       transport: stdio